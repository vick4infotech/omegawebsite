# Local development defaults (safe for running `npm run dev`).
# Copy to .env (optional). The app will also fall back to these defaults when variables are missing.

# Prisma: SQLite by default
DATABASE_URL="file:./dev.db"

# Auth.js / NextAuth v5
# You MUST change NEXTAUTH_SECRET for production.
NEXTAUTH_SECRET="dev-secret-change-me"
NEXTAUTH_URL="http://localhost:3000"

# Stripe (optional for local dev server to start)
# Add real Stripe test keys to enable the donation checkout flow.
STRIPE_SECRET_KEY="sk_test_change_me"
STRIPE_WEBHOOK_SECRET="whsec_change_me"

# Stripe price IDs (optional). If not provided, the app creates Checkout Sessions using price_data.
# STRIPE_MONTHLY_PRICE_ID=""
# STRIPE_ONE_TIME_PRICE_ID=""
